{
  "//": "Firebase Realtime Database 보안 규칙 — 보안 1단계(Google 로그인 신원 연결)용.",
  "//2": "적용: Firebase 콘솔 → Realtime Database → 규칙(Rules)에 아래 rules 객체를 붙여넣고 게시(Publish).",
  "//3": "추가로 콘솔 → Authentication → Sign-in method 에서 Google 사용 설정,",
  "//4": "Authentication → Settings → Authorized domains 에 앱 도메인(lee7120-creator.github.io) 추가 필요.",
  "//5": "1단계에서는 users/*가 아직 공개(기존 팀원 흐름 유지). 전원 계정 연결 후 2단계에서 claims 기반으로 잠급니다.",
  "rules": {
    "users": {
      ".read": true,
      ".write": true
    },
    "teamDirectory": {
      ".read": true,
      ".write": true
    },
    "userDirectory": {
      ".read": true,
      ".write": true
    },
    "claims": {
      ".read": true,
      "$name": {
        ".write": "auth != null && (data.val() === null || data.val() === auth.uid)"
      }
    },
    "accounts": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
